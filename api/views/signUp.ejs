<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign up</title>
    <%- include("shared/links") ; %>
    <style>
        body{
            background-color: #0e1111; 
        }
        #create{
            /* border: 2px solid #007bff;  */
            background-color: lightseagreen;
            border-radius: 15px;
            margin-top: 75px;
            width: 450px;
            padding: 20px;
        }
        .form-floating{
            padding: 0px 4px;
        }
        #labelnep, .form-label, .form-check-label{
            font-weight: 500;
        }
        .form-check{
            margin-bottom: 5px;
        }
        .datebox{
            margin-top: 5px;
        }
        #submitBtn{
            /* width: 100%; */
            font-weight: 600;
            border-radius: 8px;
            padding: 10px, 14px;
        }
    </style>
</head>
<body>
    <div class="container" id="create">
        <h2 class="mb-3 fw-normal text-center">Create an account</h2>
        <form onsubmit="signUp(event)" class="row gy-3 gx-3">
            <div class="col-md-12 form-floating mb-3">
                <input type="text" class="form-control" name="floatingName" placeholder="Smith">
                <label for="floatingName" id="labelnep">Username</label>
            </div>
            <div class="col-md-12 form-floating mb-3">
                <input type="email" class="form-control" name="floatingEmail" placeholder="name@example.com">
                <label for="floatingEmail" id="labelnep">Email address</label>
            </div>
            <div class="col-md-12 form-floating">
                <input type="password" class="form-control" name="floatingPassword" placeholder="Password">
                <label for="floatingPassword" id="labelnep">Password</label>
            </div>
            <div class="col-md-6">
                <label class="form-label">Gender</label>
                <div class="form-check">
                    <input class="form-check-input" type="radio" name="gender" id="male" value="Male">
                    <label class="form-check-label" for="male">Male</label>
                </div>
                <div class="form-check">
                    <input class="form-check-input" type="radio" name="gender" id="female" value="Female">
                    <label class="form-check-label" for="female">Female</label>
                </div>
            </div>  
            <div class="col-md-12 text-center mt-3">
                <button type="submit" id="submitBtn" class="btn btn-warning">Submit</button>
            </div>
        </form>
    </div>


   <script src="/js/signUp.js"></script>
</body>
</html>